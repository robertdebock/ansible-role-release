Vagrant.configure("2") do |config|


  config.vm.define "reference-archlinux" do |config|
    config.vm.box = "archlinux/archlinux"
  end

  config.vm.define "reference-alpine" do |config|
    config.vm.box = "alpine/alpine64"
  end

  config.vm.define "reference-centos-6" do |config|
    config.vm.box = "centos/6"
  end

  config.vm.define "reference-centos-7" do |config|
    config.vm.box = "centos/7"
  end

  config.vm.define("reference-debian-8") do |config|
    config.vm.box = "debian/jessie64"
  end

  config.vm.define("reference-debian-9") do |config|
    config.vm.box = "debian/stretch64"
  end

  config.vm.define "reference-fedora-26" do |config|
    config.vm.box = "fedora/26-cloud-base"
  end

  config.vm.define "reference-fedora-27" do |config|
    config.vm.box = "fedora/27-cloud-base"
  end

  config.vm.define "reference-opensuse-43.2" do |config|
    config.vm.box = "opensuse/openSUSE-42.2-x86_64"
  end

  config.vm.define "reference-opensuse-42.3" do |config|
    config.vm.box = "opensuse/openSUSE-42.3-x86_64"
  end

  config.vm.define "reference-ubuntu-trusty" do |config|
    config.vm.box = "ubuntu/trusty64"
  end

  config.vm.define "reference-ubuntu-zesty" do |config|
    config.vm.box = "ubuntu/zesty64"
  end

end
