Vagrant.configure("2") do |config|

  config.vm.define "reference-centos-7" do |config|
    config.vm.box = "centos/7"
  end

  # CentOS 8 has not been released.
  # config.vm.define "reference-centos-latest" do |config|
  #   config.vm.box = "centos/8"
  # end

end
